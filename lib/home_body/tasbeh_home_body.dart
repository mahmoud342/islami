import 'package:flutter/material.dart';

class TasbehHomeBody extends StatelessWidget {
  const TasbehHomeBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.red,
      ),
    );
  }
}
